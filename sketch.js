var canvas;
var database;
var gameState=0;
var playerCount;
var formObj, gameObj, playerObj;

function setup(){
    canvas = createCanvas(400,400);
    database=firebase.database();
}

function draw(){
  
}